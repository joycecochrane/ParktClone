<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Parkt</title>
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>



    </head>
    <body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12" style="width=device-width height=device-height">
                <nav class="navbar navbar-inverse navbar-fixed-top">
                    <div class="container-fluid">
                        <div class="col-md-1 navbar-header">
                            <%= link_to 'Parkt', :root, class: 'navbar-brand' %>
                        </div>
                        <div>
                            <ul class="nav navbar-nav">
                                <% if current_user %>
                                    <li class="active"> <%= link_to "About us", :welcome_aboutUs %></li>
                                <%end%>
                            </ul>
                            <ul class="nav navbar-nav navbar-right">
                                <li>
                                    <% if current_user %>
                                    <a href="#">
                                        <span class="glyphicon glyphicon-user"></span>
                                        <%= current_user.name %></a>
                                    <%else%>
                                    <a href="#">
                                        <span class="active glyphicon glyphicon-chevron-right"></span>
                                        <span class="active glyphicon glyphicon-chevron-right"></span>
                                        <span class="active glyphicon glyphicon-chevron-right"></span>
                                        </a>
                                    <%end%>
                                </li>

                                    <li>
                                        <% if current_user %>
                                        <a href="signout">
                                            <span class="active glyphicon glyphicon-log-out"></span> Log Out</a>

                                    <% else %>

                                        <a href="/auth/twitter">
                                            <span class="active glyphicon glyphicon-log-in"></span> Log In</a>

                                    <%end%>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>
        </div>

    <div class="container-fluid" id="main-content-container" style="margin-top:50px">
            	<%=yield%>
	</div>


        <!--*TWITTERTUTORIAL*
            <% if current_user %>
                <%= link_to image_tag("TwitterSignOutButton.jpg"), 'signout' %>
                <p>Signed in as <%= current_user.name %></p>
            <% else %>
                <%= link_to image_tag("TwitterSignInButton.jpg"), 'auth/twitter' %>
            <%end%>
            *TWITTERTUTORIAL*-->
   </div>

    </body>
</html>
